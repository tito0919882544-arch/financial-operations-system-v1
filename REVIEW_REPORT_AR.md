# تقرير المراجع — النسخة الحالية

## نطاق المراجعة
هذه النسخة مخصصة لمراجعة **مركز الصيانة والتشغيل والطبقة التشغيلية التي أُدخلت معه فقط**. لم يتم إدخال بقية البنود المعتمدة خارج هذا النطاق.

## نتيجة الفحص الساكن
- Python AST: يجب أن يمر على جميع ملفات Python.
- بنية المشروع والملفات الأساسية: تم التحقق منها.
- مسارات مركز الصيانة والوظائف التشغيلية الأساسية: موجودة.
- حماية سجل التدقيق من UPDATE وDELETE: موجودة في مخطط SQLite.
- استثناء Super Admin من حجب وضع الصيانة: موجود في guard.
- حفظ Theme وCSS المخصص: موجود.
- مركز الإشعارات: التخزين والمسارات والتوجيه حسب الدور موجودة.
- البحث الشامل: مسار فعلي موجود.
- المساعدة السياقية: مسار فعلي موجود.
- فحص ما قبل الإرسال: مسار فعلي موجود.
- حزمة العملية: مسار فعلي موجود.
- استكمال العملية: مسار فعلي موجود.
- مراقبة العمليات: مسار فعلي موجود.
- عزل إعدادات النثرية بالمحطة والشهر: موجود في الاستعلامات.

## ملاحظات حرجة يجب عدم إخفائها

### 1. الاختبار الساكن لا يثبت التشغيل
نجاح الفحص الساكن لا يعني أن الواجهة تعمل فعليًا في Termux. يلزم اختبار HTTP حقيقي.

### 2. وضع الصيانة
يجب إثبات عمليًا:
- Super Admin يدخل أثناء الصيانة.
- Manager/Collector يحصلان على شاشة الصيانة.
- لا يمكن تجاوز الحجب بالوصول المباشر لمسار محمي.

### 3. Theme Engine
وجود `set_setting` وحقن CSS لا يكفي. يجب:
- اختيار كل Theme.
- إعادة تحميل Dashboard وصفحة داخلية.
- تسجيل الخروج وفتح شاشة الدخول.
- إعادة تشغيل التطبيق.
- التأكد من استمرار الاختيار وتغير الشكل فعليًا.

### 4. النثرية
يجب اختبار:
إنشاء محطة → إنشاء بند/مبلغ → حفظ → إعادة فتح → إنشاء متحصل للمحطة → الدخول كمتحصل → ظهور البند الصحيح.
ويجب ألا يتحول السقف المخصص إلى الرصيد الفعلي.

### 5. الإشعارات
يجب اختبار كل مصدر إشعار:
- عملية جديدة → المدير.
- إرجاع/رفض/اعتماد → منشئ العملية.
- طلب استعادة كلمة المرور → Super Admin.
- طلب تغذية النثرية → المدير.
ثم التأكد من عدم ظهور إشعار محطة لمستخدم محطة أخرى.

### 6. البحث
يجب اختبار الصلاحيات: لا يجوز أن يكشف البحث عمليات محطة أخرى للمستخدم العادي.

### 7. حزمة العملية
يجب التأكد من أن المرفق الموجود فعليًا يدخل في الحزمة، وأن Timeline والتصحيحات تعكس السجل الحقيقي.

### 8. سجل التدقيق
يجب اختبار محاولة التعديل والحذف عبر التطبيق، وليس فقط وجود triggers في قاعدة البيانات.

## معيار المراجع
لا نعتمد الوظيفة مكتملة لمجرد وجود:
- صفحة.
- زر.
- Route.
- رسالة نجاح.

الاعتماد العملي هو:

**حفظ → تأثير فعلي → ظهور للمستخدم الصحيح → استمرار بعد إعادة الدخول → سجل تدقيق عند الحاجة.**

## خارج نطاق هذه النسخة
لم يتم تنفيذ بقية البنود المعتمدة التي طلب المستخدم عدم إدخالها في هذه المرحلة.
