# إصلاح تفعيل التصميم — طبقات عائمة

تم إصلاح مشكلتين ظهرتا أثناء الاختبار:

1. صفحات مركز الصيانة الفرعية كانت تقبل GET فقط، بينما نماذجها ترسل POST إلى `/maintenance/<section>`؛ لذلك كان Flask يعيد 405 Method Not Allowed. أصبحت route المركز الفرعية تقبل GET وPOST مع دعم وجود `/` في نهاية العنوان.
2. تصميم «طبقات عائمة» كان مسجلاً في قاعدة البيانات بدون CSS فعلي، لذلك كان زر التفعيل يسجل الإعداد لكن التغيير البصري لا يظهر. أضيفت قواعد CSS مدمجة للتصميم في `templates/base.html` مع إبقاء دعم CSS المخصص كما هو.

لم يتم تغيير المنطق المالي أو الصلاحيات أو الجداول المالية.


## الإصلاح النهائي — 2026-09-01
- إصلاح خطأ Jinja في `templates/login.html` الناتج عن كتلة `if` غير المغلقة.
- إعادة تنظيم شاشة الدخول بقالب Jinja أبسط وأكثر أمانًا من ناحية التركيب.
- اعتماد طبقة واجهة Light Glass تنفيذية بخلفية رمادي فاتح مائل للأبيض، مع كحلي مؤسسي وأزرق فولاذي ولمسات دلالية هادئة.
- إزالة سبب تجاوز بطاقات الإحصاءات لعرض الهاتف عبر `minmax(0,1fr)` وإدارة عدد الأعمدة حسب العرض.
- تحويل الشريط الجانبي على الهاتف إلى قائمة جانبية Off-canvas بدل اقتطاع مساحة المحتوى.
- إضافة زر قائمة للهاتف وBackdrop وإغلاق بالضغط خارج القائمة أو مفتاح Escape.
- منع التمرير الأفقي على مستوى الصفحة مع الإبقاء على التمرير الأفقي للجداول عند الحاجة.
- جعل تصميم `طبقات عائمة` متوافقًا مع الهوية الفاتحة الزجاجية المطلوبة بدل الخلفية الداكنة السابقة.
- لم يتم تغيير منطق العمليات المالية أو الصلاحيات أو دورة الاعتماد أو عزل النثرية.
